# SPT-Txn Attestation Anchor — Foundry config.
# The contract has zero dependencies, so no remappings/libs are needed.
[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
